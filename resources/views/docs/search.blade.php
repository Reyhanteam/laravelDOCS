@extends('docs.layout')
@section('content')
<div class="mx-auto max-w-4xl px-4 py-12 sm:px-6 lg:px-8"><h1 class="text-3xl font-bold">Search documentation</h1><form class="mt-6 flex gap-2"><input name="q" value="{{ $query }}" autofocus class="flex-1 rounded-md border border-slate-300 bg-white px-4 py-2 dark:border-slate-700 dark:bg-slate-900" placeholder="Search…"><button class="rounded-md bg-red-600 px-5 py-2 text-sm font-medium text-white">Search</button></form>@if($query!=='' )<p class="mt-8 text-sm text-slate-500">{{ count($results) }} result(s) for “{{ $query }}”.</p><div class="mt-4 divide-y divide-slate-200 dark:divide-slate-800">@forelse($results as $result)<a href="{{ route('docs.show',['path'=>$result['slug']]) }}" class="block py-5 hover:bg-slate-50 dark:hover:bg-slate-900"><div class="font-semibold">{{ $result['title'] }}</div><div class="mt-1 text-sm text-slate-500">{{ $result['subtitle'] ?? '' }}</div></a>@empty<p class="py-8 text-slate-500">No documentation page matched your search.</p>@endforelse</div>@endif</div>
@endsection
